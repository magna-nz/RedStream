<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>In-process test fake for RedStream — unit-test handlers and reaper logic without spinning up a real Redis.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\RedStream\RedStream.csproj" />
  </ItemGroup>

</Project>
